# langchain-chat

Enterprise-grade AI ChatBot framework based on LangChain.

## Requirements

- Python >= 3.10

## Quick Start

```bash
# Install dependencies
pip install -e ".[dev]"

# Run entry point
python -m langchain_chat.main
```

## Project Status

**Step 1** — Project initialization (current).

